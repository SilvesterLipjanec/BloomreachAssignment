import { InputValueTypePipe } from './input-value-type.pipe';

describe('InputValueTypePipe', () => {
  it('create an instance', () => {
    const pipe = new InputValueTypePipe();
    expect(pipe).toBeTruthy();
  });
});
